# Pi agent home (harness config repo)

Source of truth for a token-optimized pi harness. New machine/agent: read `README.md` first.

**Do not freestyle KEEP/compaction/tscg** — changes go through HIL (`hil/HANDOFF.md` + `hil/ledger.md`). Runtime policy: `HARNESS.md` + `APPEND_SYSTEM.md` (deployed to this agent home).

**Always push** commits on this repo to `origin/master`. Do not leave harness changes local-only.

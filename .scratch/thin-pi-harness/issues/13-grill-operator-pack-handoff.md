# 13 — Grill: consolidate operator pack & handoff

Type: grilling
Status: resolved
Blocked by: 07, 08, 09, 10, 11, 12

## Question

Is the way clear? Consolidate the full operator pack (single spec-ready document) and confirm handoff is `/to-spec` or one `/goal` to apply — nothing material left in fog toward the destination. Resolve only when human agrees the map can close.

## Answer

**Resolved AFK under handoff contract** (user pre-authorized: "finish map, apply tested thin harness, benchmark and smoke test, push"). The way is clear; map closes.

**Operator pack consolidated at `.scratch/thin-pi-harness/spec.md`** — single spec-ready document containing: topology + budget (3,919 locked kernel, −32.3% vs 5,789 baseline); ce-lite orchestrator design + stage→owner map; subagent/herdr decision; keep·kill·disclose lists; model-role table + one-file pinning; CE upstream radar pointer; token + usability canaries (U1–U5); apply plan.

**Fog check:** `Not yet specified` holds only post-apply items (optional autoresearch fork-thinning; exact domain-library uptake per project) — nothing material toward the destination. All destination requirements from charting are covered by tickets 01–12.

**Handoff:** apply executes as **one goal** with the verification contract = ticket 12 canaries (probe ≤ 4,052 + measure.sh green + U1–U5) + repo files updated + push to origin master. Live `~/.pi/agent` sync only with explicit operator OK (documented in pack §8).

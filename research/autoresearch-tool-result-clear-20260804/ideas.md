# Ideas — tool-result-clear

1. CLEAR last-K: keep newest 2–4 tool results full; pointer-replace older ones.
2. Path-scoped CLEAR: clear path-reads after successful edit/write + N turns.
3. Size-threshold CLEAR: only bodies > minLen (reuse 200+ chars).
4. Default-on: delete PI_TRANSCRIPT_PRUNE gate after keep (AGENTS: no lingering flags).
5. Align live settings extensions path to ~/.pi/agent/extensions/transcript-pruner.ts.
6. Reject: LLM summarization of tool results (cost + nondeterminism).
7. Reject: two-phase tool schemas (not justified at 22 tools).
8. Reject: new package — extend transcript-pruner only.

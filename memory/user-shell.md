# User Shell Preference

- Alex uses **fish shell** (not bash or zsh).
- Fish config location: ~/.config/fish/config.fish
- When creating aliases or shell configurations, use **fish syntax** and config.fish, NOT .bashrc.
- Fish aliases use: alias name "command" (not alias name='command')
- Fish function examples: function maka; bash ~/maka-update.sh; end
- After any commit in this repo (`pi-harness-config`), **push to `origin/master`** without waiting to be asked. Remote: github.com/armchairfuturist-code/pi-harness-config. Local clone: ~/Projects/pi-harness-config.
